<!DOCTYPE html>
<html>
<head>
  <title>Trevorspace Survey - Admin</title>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.2/jquery.min.js"></script>
  <script src="http://ajax.googleapis.com/ajax/libs/jqueryui/1.8.18/jquery-ui.min.js"></script>

  <%= javascript_include_tag "jquery.tablesorter-2.0.5.js" %>
  <%= stylesheet_link_tag "ui-lightness/jquery-ui-1.8.19.custom.css" %>
  <%= stylesheet_link_tag "tablesorter-themes/blue/style.css" %>
  <%= csrf_meta_tags %>

  <script type="text/javascript">
    $(document).ready(function() {
      $('.tablesorter').tablesorter({widgets: ['zebra']});
    });
  </script>

  <%= yield :head %>
</head>
<body>
  
  <h2>
    <%= link_to "Survey Admin", :controller => 'survey_admin/home', :action => 'index' %> - 
    <% if @survey %>
    <%= link_to @survey.title, :controller => 'survey_admin/responses', :action => 'list', :survey_access_code => @survey.access_code %>
    <% end %>
  </h2>

  <% if @survey %>
    <div>
      <span style="font-size: large; font-weight: bold;">Responses - </span>
      <%= @survey.total_completed_responses %> completed,
      <%= @survey.response_sets.size - @survey.total_completed_responses %> incomplete,
      <%= @survey.response_sets.size %> total
    </div>
  <% end %>

  <hr />
  
  <%= yield %>

</body>
</html>
